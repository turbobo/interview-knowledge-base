# AI Agent / LLM Agent 面试题总结

> 来源：小林笔记 — https://xiaolinnote.com/agent/concept/agent.html  
> 整理时间：2026-08-22  
> 适用场景：AI 应用开发面试、大模型 Agent 方向面试、技术分享

---

## 一、概念定义

### 1.1 LLM（大语言模型）

基于海量文本训练的语言规律预测模型。虽具备强大的文本生成与理解能力，但存在以下局限：

- 无法执行物理操作
- 缺乏跨会话记忆
- 无法直接调用外部工具
- 缺乏自主规划能力

### 1.2 Agent（智能体）

**定义**：LLM 在循环中自主使用工具的系统。

它赋予大模型执行操作、记忆上下文及规划任务的能力，使其从被动响应的"顾问"转变为主动推进目标的"项目经理"。

### 1.3 Workflow（工作流）

**定义**：LLM 和工具通过预定义的代码路径进行编排的系统。

**与 Agent 的核心差异**：流程控制权归属——Workflow 由代码固化，Agent 由大模型动态决策。

| 维度 | Workflow | Agent |
|------|----------|-------|
| 控制权 | 代码固化 | 大模型动态决策 |
| 灵活性 | 低 | 高 |
| 可靠性 | 高 | 中 |
| 适用场景 | 步骤明确的任务 | 开放式复杂任务 |

---

## 二、核心组件

### 2.1 大脑（LLM）

负责意图理解、逻辑推理与行动决策的核心引擎。

### 2.2 规划模块

将复杂宏观目标拆解为可执行的具体步骤序列。

### 2.3 记忆模块

- **短期记忆**：维持当前任务连贯性的上下文
- **长期记忆**：跨对话持久化存储的知识与经验

### 2.4 工具模块

充当系统的执行器官，实现与外部 API、数据库、搜索引擎等环境的交互。

---

## 三、工作原理

Agent 的底层运转依赖于将自然语言意图转化为结构化操作：

```
用户指令 → LLM 分析意图 → 决定是否调用工具 → 提取结构化参数 
→ 宿主应用执行函数 → 返回客观结果 → LLM 生成回复
```

这一过程在循环中不断迭代（思考、行动、观察），直至目标达成。

**关键特征**：
- 意图识别：理解用户真实需求
- 工具选择：决定何时、调用什么工具
- 参数提取：从自然语言中提取结构化参数
- 结果整合：将工具返回结果融合进自然语言回复

---

## 四、设计模式

### 4.1 ReAct（推理 + 行动）

**模式**：采用思考、行动、观察的三步循环，边想边做。

**优点**：
- 高透明度（思考过程可见）
- 高灵活性（动态调整策略）

**缺点**：
- Token 消耗较大
- 易陷入死循环

**适用**：需要动态决策、探索性任务。

### 4.2 Plan-and-Execute（规划 + 执行）

**模式**：先制定全局计划再逐步执行。

**优点**：
- 显著降低推理成本
- 适合步骤明确的任务

**缺点**：
- 应对突发变化的灵活性较弱

**适用**：目标清晰、步骤可预判的任务。

### 4.3 Reflection（反思机制）

**模式**：任务完成后引入自我审查或双智能体交叉评审。

**优点**：
- 提升代码或专业文档的输出质量
- 减少低级错误

**适用**：代码生成、文档撰写、方案设计。

### 4.4 Multi-Agent（多智能体协作）

**模式**：将复杂任务分配给多个专业化角色协同完成。

**优点**：
- 专业化分工，提升效率
- 角色边界清晰

**缺点**：
- 系统复杂度增加
- 协调成本上升

**注意**：避免过早引入，以防系统过度复杂化。

---

## 五、关键技术点

### 5.1 Function Call（函数调用）

**定位**：Agent 的原子操作基础。

**解决的问题**：
- 模型"何时调用"（意图识别）
- "提取何种参数"（参数抽取）

**意义**：使大模型具备驱动外部系统的能力。

### 5.2 MCP（模型上下文协议）

**定位**：被誉为"AI 界的 USB-C 接口"。

**架构**：Host、Client、Server 三层。

**核心价值**：将 N×M 的工具集成难题转化为 N+M 的标准化连接，支持工具的动态发现。

**解决的问题**：
- 工具接入成本高
- 多模型多工具的适配复杂度

### 5.3 Skills（技能）

**定位**：以自然语言编写的指令文件（如 Markdown）。

**作用**：为模型注入领域专业知识与最佳实践。

**解决的问题**："如何规范做事"的方法论问题。

**示例**：
- 代码审查规范
- SQL 优化最佳实践
- 自动化部署流程

### 5.4 A2A（Agent-to-Agent）协议

**定位**：专注于智能体间的横向通信。

**核心概念**：
- **Agent Card**：智能体的名片（能力、接口、版本）
- **Task**：任务生命周期管理
- **Artifact**：制品（任务产出物）

**意义**：实现跨框架、跨平台的智能体互相发现与任务委派。

---

## 六、应用场景

### 6.1 混合架构客服系统

**架构**：
- 整体工单流转采用 Workflow 保障可靠性
- 复杂疑难问题动态路由至 Agent 自主分析解决

**优势**：兼顾稳定性与灵活性。

### 6.2 企业级跨部门协同

**场景**：借助 A2A 协议，实现 HR、IT、财务等异构智能体在员工入职等长周期任务中的自动化协作。

**优势**：打破系统孤岛，实现端到端自动化。

### 6.3 专业领域工程化

**场景**：利用 Skills 封装代码审查、SQL 优化或自动化部署等专家经验。

**优势**：确保团队级 AI 辅助开发的规范与质量。

### 6.4 开放式复杂任务

**场景**：竞品分析报告生成、自动化差旅规划等需要动态调整策略、调用多源数据的场景。

**优势**：Agent 的自主决策能力应对不确定性。

---

## 七、面试高频问题

### Q1. Agent 和 Workflow 的区别？

**答**：核心差异在于流程控制权归属。Workflow 由代码固化流程，Agent 由大模型动态决策。Workflow 适合步骤明确的任务（高可靠性），Agent 适合开放式复杂任务（高灵活性）。

### Q2. ReAct 和 Plan-and-Execute 如何选择？

**答**：
- ReAct：需要动态决策、探索性任务，思考过程透明
- Plan-and-Execute：目标清晰、步骤可预判，推理成本更低

### Q3. MCP 解决了什么问题？

**答**：MCP 解决了工具接入的复杂度问题。传统方式下，N 个模型 × M 个工具需要 N×M 次集成；MCP 通过标准化协议，将复杂度降为 N+M，并支持工具的动态发现。

### Q4. 如何避免 Agent 陷入死循环？

**答**：
- 设置最大迭代次数
- 引入反思机制（Reflection）
- 工具调用结果校验
- 超时熔断

### Q5. Multi-Agent 的适用场景？

**答**：适合复杂任务需要多角色协作的场景（如软件开发中的需求分析、设计、编码、测试分工）。但需避免过早引入，简单任务用单 Agent 即可。

### Q6. Function Call 和 MCP 的关系？

**答**：Function Call 是 Agent 调用工具的原子操作（单次调用）；MCP 是工具接入的标准化协议（系统级规范）。Function Call 解决"怎么调"，MCP 解决"怎么接"。

### Q7. Skills 和 Prompt 的区别？

**答**：Prompt 是单次对话的上下文指令；Skills 是持久化的领域知识文件（如 Markdown），可跨会话复用，为模型注入规范化的最佳实践。

---

## 八、延伸阅读

- [小林笔记 — Agent 概念](https://xiaolinnote.com/agent/concept/agent.html)
- [OpenAI Function Calling 文档](https://platform.openai.com/docs/guides/function-calling)
- [Model Context Protocol (MCP)](https://modelcontextprotocol.io/)
- [Agent-to-Agent (A2A) Protocol](https://github.com/google/A2A)

---

祝面试顺利！
