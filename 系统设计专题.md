# 系统设计专题

> 社招面试核心考点，覆盖 6 大高频系统设计题。
> 每题包含：核心思路、架构图、关键技术点、面试追问。

---

## 一、秒杀系统

### 核心挑战

瞬时高并发（10 万+ QPS）、库存防超卖、防止恶意刷单。

### 架构分层

```
用户请求
  │
  ▼
┌─────────────────── CDN 层 ───────────────────┐
│  静态资源（页面/图片/JS）缓存到边缘节点          │
│  减少 90% 请求到达后端                          │
└──────────────────────────────────────────────┘
  │
  ▼
┌─────────────────── 网关层 ───────────────────┐
│  · 限流（令牌桶/滑动窗口，1000 QPS/用户）       │
│  · 黑名单（IP/用户维度）                        │
│  · 验证码（削峰 + 防机器人）                     │
└──────────────────────────────────────────────┘
  │
  ▼
┌─────────────────── 服务层 ───────────────────┐
│  · Redis 预扣库存（Lua 脚本保证原子性）          │
│  · 库存 > 0 → 发送 MQ 消息                     │
│  · 库存 ≤ 0 → 直接返回"已抢光"                 │
└──────────────────────────────────────────────┘
  │
  ▼
┌─────────────────── 异步层 ───────────────────┐
│  · MQ 消费：创建订单 + 扣减 DB 库存             │
│  · 超时未支付 → 库存回滚                        │
└──────────────────────────────────────────────┘
```

### 关键技术点

| 技术点 | 方案 | 说明 |
|--------|------|------|
| 库存预扣 | Redis + Lua 脚本 | 原子操作，避免超卖 |
| 防超卖 | DB 乐观锁 `UPDATE stock SET count=count-1 WHERE id=1 AND count>0` | 最终兜底 |
| 削峰 | MQ 异步下单 | 将瞬时流量转为匀速消费 |
| 限流 | 令牌桶 / 滑动窗口 | 网关层 + 服务层双重限流 |
| 防刷 | 验证码 + 用户维度限频 | 每个用户 N 秒只能请求 1 次 |
| 热点隔离 | 独立集群部署 | 秒杀服务不影响主站 |

### Redis 预扣库存 Lua 脚本

```lua
local stock = tonumber(redis.call('get', KEYS[1]))
if stock > 0 then
    redis.call('decr', KEYS[1])
    return 1  -- 扣减成功
else
    return 0  -- 库存不足
end
```

### 面试追问

**Q：如果 Redis 和 DB 库存不一致怎么办？**
> 以 Redis 为准做预扣，DB 做最终一致性校验。MQ 消费失败时重试，超过重试次数进死信队列人工处理。

**Q：秒杀结束后如何统计？**
> Redis 记录每个用户的秒杀结果，异步写入 DB 做数据分析。

---

## 二、短链系统

### 核心挑战

海量短链生成（每秒 10 万+）、低延迟跳转、长期存储。

### 架构设计

```
用户提交长链
      │
      ▼
┌─────────────────── 发号器 ───────────────────┐
│  生成唯一 ID（雪花算法 / 数据库自增）            │
│  转 Base62 编码 → 短码（如 aB3xK9）           │
└──────────────────────────────────────────────┘
      │
      ▼
┌─────────────────── 存储层 ───────────────────┐
│  KV 存储：短码 → 长链                         │
│  · 热点短链缓存到 Redis                        │
│  · 冷数据存 DB / HBase                        │
└──────────────────────────────────────────────┘
      │
      ▼
┌─────────────────── 跳转层 ───────────────────┐
│  302 重定向（临时）或 301（永久）               │
│  · 302：方便统计每次访问                        │
│  · 301：浏览器缓存，减少服务端压力               │
└──────────────────────────────────────────────┘
```

### 发号器方案对比

| 方案 | 优点 | 缺点 |
|------|------|------|
| 数据库自增 | 简单可靠 | 性能瓶颈（单点） |
| 雪花算法 | 高性能、分布式 | 时钟回拨问题 |
| Redis INCR | 高性能 | 需持久化，宕机丢号 |
| 号段模式 | 批量获取，减少 DB 访问 | 实现复杂 |

### Base62 编码

```
ID = 12345678
Base62 编码 → "dnh3k"（62 = 26小写 + 26大写 + 10数字）
短链 = https://s.xxx.com/dnh3k
```

### 面试追问

**Q：哈希碰撞怎么处理？**
> 发号器保证全局唯一 ID，不依赖哈希。如果用哈希方案，碰撞时加盐重试或换算法。

**Q：短链被恶意滥用怎么办？**
> 域名黑名单 + 内容审核 + 用户举报机制。

---

## 三、Feed 流系统

### 核心挑战

大 V 粉丝多（写扩散成本高）、普通用户关注多（读延迟高）。

### 推拉模式对比

| 模式 | 原理 | 优点 | 缺点 | 适用 |
|------|------|------|------|------|
| **推模式**（写扩散） | 发布时写入每个粉丝的 Timeline | 读取快（直接读自己 Timeline） | 大 V 写放大严重 | 普通用户 |
| **拉模式**（读扩散） | 读取时聚合关注列表的内容 | 写入快 | 读取时聚合慢 | 大 V |
| **推拉结合** | 大 V 用拉，普通用户用推 | 平衡读写 | 实现复杂 | 混合场景 |

### 推拉结合架构

```
用户 A 发布内容
      │
      ▼
┌─────────────────── 判断逻辑 ──────────────────┐
│  粉丝数 < 1000 → 推模式（写入粉丝 Timeline）    │
│  粉丝数 ≥ 1000 → 拉模式（只写自己 Feed 箱）     │
└──────────────────────────────────────────────┘
      │
      ▼
┌─────────────────── 读取时 ───────────────────┐
│  1. 读取自己的 Timeline（推模式内容）            │
│  2. 拉取关注的大 V 最新内容（拉模式）            │
│  3. 按时间排序合并                              │
└──────────────────────────────────────────────┘
```

### 存储方案

| 组件 | 用途 | 说明 |
|------|------|------|
| Redis ZSet | Timeline 存储 | `ZADD timeline:{userId} timestamp postId` |
| Redis ZSet | 大 V Feed 箱 | `ZADD feed:{vId} timestamp postId` |
| DB | 内容详情 | postId → 完整内容 |
| MQ | 异步推送 | 发布后异步写入粉丝 Timeline |

### 面试追问

**Q：Timeline 分页怎么避免重复/遗漏？**
> 用游标分页（Cursor），记录上次读取的最后一条 ID，下次从该 ID 之后读取。避免 offset 分页在数据变动时的重复/遗漏。

**Q：已读状态怎么记录？**
> 用户维度维护已读集合（Redis Set 或 Bloom Filter），读取时过滤。

---

## 四、分布式 ID 生成

### 方案对比

| 方案 | 长度 | 有序性 | 性能 | 复杂度 |
|------|------|--------|------|--------|
| UUID | 36 字符 | 无序 | 高 | 低 |
| 数据库自增 | 8 字节 | 递增 | 低（瓶颈） | 低 |
| 雪花算法 | 8 字节 | 趋势递增 | 极高 | 中 |
| Redis INCR | 8 字节 | 递增 | 高 | 中 |
| 号段模式 | 8 字节 | 递增 | 高 | 高 |

### 雪花算法（Snowflake）

```
64 位 ID 结构：
┌─────┬──────────────┬────────────┬──────────────┐
│ 1位 │    41位       │   10位     │    12位       │
│ 符号│  时间戳(ms)   │  机器ID    │   序列号      │
│  0  │  ~69年        │  1024台    │  每ms 4096个  │
└─────┴──────────────┴────────────┴──────────────┘
```

**优点**：高性能（单机 400 万/秒）、趋势递增、不依赖外部组件。

**时钟回拨问题**：服务器时钟同步时可能回拨，导致生成重复 ID。解决方案：
- 等待时钟追上（简单但阻塞）
- 扩展位记录回拨次数
- 使用 ZK/etcd 分配机器 ID，避免冲突

### 号段模式（Leaf-segment）

```
每次从 DB 获取一个号段（如 1000-2000），用完再取下一个。
减少 DB 访问频率，适合高并发场景。

DB 表：
┌──────┬──────────┬──────────┬──────────┐
│  biz │ max_id   │  step    │  version │
├──────┼──────────┼──────────┼──────────┤
│ order│  2000    │  1000    │    3     │
└──────┴──────────┴──────────┴──────────┘

应用内存：[1001, 1002, ..., 2000] → 用完取 [2001, ..., 3000]
```

### 面试追问

**Q：雪花算法的机器 ID 怎么分配？**
> 静态配置（适合小规模）；ZK/etcd 动态注册（适合大规模集群）。

**Q：分库分表后 ID 怎么保证唯一？**
> 用分布式 ID 生成器（雪花/号段），不用数据库自增。

---

## 五、排行榜系统

### 核心挑战

实时更新、海量数据、高并发查询 Top N。

### 方案：Redis ZSet

```
┌─────────────────── Redis ZSet ───────────────┐
│  Key: leaderboard:{type}:{period}             │
│  Member: userId                               │
│  Score: 积分/金额/次数                         │
│                                               │
│  ZADD leaderboard:game:daily 1500 "user123"   │
│  ZINCRBY leaderboard:game:daily 100 "user123" │
│  ZREVRANGE leaderboard:game:daily 0 99        │
│  ZREVRANK leaderboard:game:daily "user123"    │
└──────────────────────────────────────────────┘
```

| 操作 | 命令 | 时间复杂度 |
|------|------|-----------|
| 更新分数 | `ZINCRBY key score member` | O(log N) |
| 查 Top N | `ZREVRANGE key 0 N-1` | O(log N + M) |
| 查排名 | `ZREVRANK key member` | O(log N) |
| 查分数 | `ZSCORE key member` | O(1) |

### 多维度排行榜

```
日榜：leaderboard:game:2026-01-15  （每天一个 key，自动过期）
周榜：leaderboard:game:2026-W03    （每周一个 key）
月榜：leaderboard:game:2026-01     （每月一个 key）
总榜：leaderboard:game:total       （永不过期）
```

### 面试追问

**Q：排行榜数据量很大怎么办？**
> 只保留 Top 10000 在 Redis，超出部分淘汰或归档到 DB。查询 10000 名之后的排名走 DB。

**Q：历史排行榜怎么查？**
> 每天定时将 Redis 数据快照到 DB（MySQL/ClickHouse），支持按日期查询历史排名。

---

## 六、分布式限流

### 算法对比

| 算法 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **固定窗口** | 固定时间窗口内计数 | 简单 | 临界点突发（窗口切换瞬间 2 倍流量） |
| **滑动窗口** | 窗口随时间滑动 | 平滑 | 实现复杂 |
| **漏桶** | 固定速率流出 | 流量整形 | 无法应对突发 |
| **令牌桶** | 固定速率生成令牌，请求消耗令牌 | 允许突发 | 实现稍复杂 |

### 令牌桶实现（Redis + Lua）

```lua
-- KEYS[1]: 令牌桶 key
-- ARGV[1]: 桶容量
-- ARGV[2]: 每秒生成令牌数
-- ARGV[3]: 当前时间戳
-- ARGV[4]: 请求消耗令牌数

local bucket = redis.call('hmget', KEYS[1], 'tokens', 'last_time')
local tokens = tonumber(bucket[1]) or ARGV[1]
local last_time = tonumber(bucket[2]) or ARGV[3]

local delta = math.max(0, ARGV[3] - last_time)
tokens = math.min(ARGV[1], tokens + delta * ARGV[2] / 1000)

if tokens >= tonumber(ARGV[4]) then
    tokens = tokens - tonumber(ARGV[4])
    redis.call('hmset', KEYS[1], 'tokens', tokens, 'last_time', ARGV[3])
    return 1  -- 允许通过
else
    return 0  -- 限流
end
```

### 多层限流架构

```
┌─────────────── 第 1 层：客户端 ───────────────┐
│  按钮防抖 + 请求频率限制（前端）                 │
└──────────────────────────────────────────────┘
          │
          ▼
┌─────────────── 第 2 层：网关 ─────────────────┐
│  Nginx limit_req / Sentinel / Kong            │
│  全局 QPS 限制 + IP 维度限流                    │
└──────────────────────────────────────────────┘
          │
          ▼
┌─────────────── 第 3 层：服务 ─────────────────┐
│  Redis + Lua 令牌桶                            │
│  用户维度 + 接口维度细粒度限流                    │
└──────────────────────────────────────────────┘
```

### 面试追问

**Q：分布式环境下限流数据一致性怎么保证？**
> Redis + Lua 保证单 key 原子性。多 key 场景用 Redisson 分布式锁或 CRDT。

**Q：限流被触发后怎么降级？**
> 返回友好提示（"系统繁忙，请稍后重试"）+ 降级到静态缓存页 + 告警通知运维。

---

## 七、通用设计方法论

### 面试 4 步法

```
1. 需求澄清（5 min）
   · 功能需求：核心功能有哪些？
   · 非功能需求：QPS / 延迟 / 可用性 / 数据量？
   · 约束：团队规模、时间、技术栈？

2. 高层设计（15 min）
   · 画出核心组件和数据流
   · 定义 API 接口
   · 选择存储方案（DB / Cache / MQ）

3. 深入设计（15 min）
   · 选择 1-2 个核心模块深入
   · 讨论 trade-off（为什么选 A 不选 B）
   · 考虑扩展性和容错

4. 扩展讨论（5 min）
   · 监控和告警
   · 如何扩展（水平 / 垂直）
   · 故障场景和处理方案
```

### 常用技术选型速查

| 场景 | 推荐方案 | 备选 |
|------|----------|------|
| 高并发缓存 | Redis Cluster | Memcached |
| 消息队列 | RocketMQ（国内）/ Kafka（日志） | RabbitMQ |
| 关系型数据库 | MySQL（主从/分库分表） | PostgreSQL |
| NoSQL | MongoDB（文档）/ HBase（列族） | Cassandra |
| 搜索 | Elasticsearch | Solr |
| 对象存储 | OSS / S3 | MinIO |
| 分布式协调 | ZooKeeper / etcd | Consul |
| 配置中心 | Nacos / Apollo | Spring Cloud Config |
| 注册中心 | Nacos | ZooKeeper / Consul |
| 网关 | Spring Cloud Gateway / Kong | Nginx |

---

## 八、面试高频追问汇总

| 追问 | 考察点 |
|------|--------|
| 这个系统的瓶颈在哪？ | 对系统全局的理解 |
| 如果流量增长 10 倍怎么办？ | 水平扩展能力 |
| 如果某个组件挂了怎么办？ | 容错和降级设计 |
| 数据一致性怎么保证？ | 分布式事务 / 最终一致性 |
| 为什么选这个方案而不是 XX？ | 技术选型和 trade-off 思维 |
| 怎么监控和告警？ | 可观测性设计 |
| 如何灰度发布？ | 发布策略和风险控制 |
