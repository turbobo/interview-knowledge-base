# JVM 内存区域全景图

> 来源：[JavaGuide — JVM 内存区域详解](https://javaguide.cn/java/jvm/memory-area.html)
> 配套可视化图：[JVM内存区域全景图.html](./JVM内存区域全景图.html)（浏览器打开查看）

---

## 总览

JVM 运行时数据区分为 **线程私有** 和 **线程共享** 两大类，加上一个非规范的 **直接内存**。

```
┌─────────────────────────────────────────────────────────────┐
│                    JVM 运行时数据区                           │
├──────────────────────────┬──────────────────────────────────┤
│      线程私有（3 个）     │         线程共享（2 个）          │
│                          │                                  │
│  ┌────────────────────┐  │  ┌────────────────────────────┐  │
│  │ 程序计数器 (PC)     │  │  │ 堆 (Heap)                  │  │
│  │ · 唯一不会 OOM     │  │  │ · Eden + S0 + S1 + Old     │  │
│  └────────────────────┘  │  │ · GC 主战场，会 OOM        │  │
│  ┌────────────────────┐  │  └────────────────────────────┘  │
│  │ Java 虚拟机栈       │  │  ┌────────────────────────────┐  │
│  │ · 栈帧：局部变量表  │  │  │ 方法区 (Method Area)        │  │
│  │   操作数栈/动态链接 │  │  │ · JDK7: 永久代 PermGen     │  │
│  │   方法返回地址      │  │  │ · JDK8+: 元空间 Metaspace  │  │
│  │ · 会 StackOverflow │  │  │ · 含运行时常量池            │  │
│  └────────────────────┘  │  │ · 字符串池 JDK7 移至堆中    │  │
│  ┌────────────────────┐  │  └────────────────────────────┘  │
│  │ 本地方法栈          │  │                                  │
│  │ · HotSpot 中与栈合并│  ├──────────────────────────────────┤
│  │ · 会 StackOverflow │  │  非 JVM 规范：直接内存            │
│  └────────────────────┘  │  · NIO DirectByteBuffer          │
│                          │  · 堆外分配，减少数据拷贝          │
│                          │  · 受物理内存限制，会 OOM          │
└──────────────────────────┴──────────────────────────────────┘
```

---

## 一、线程私有区域（随线程创建/销毁）

### 1. 程序计数器（Program Counter）

| 属性 | 说明 |
|------|------|
| 作用 | 字节码行号指示器，记录当前执行的指令地址 |
| 存放 | 字节码指令地址（native 方法时为 Undefined） |
| 用途 | 线程切换后恢复执行位置、控制流跳转 |
| OOM | **不会** — JVM 规范中唯一不会 OOM 的区域 |

### 2. Java 虚拟机栈（JVM Stack）

| 属性 | 说明 |
|------|------|
| 作用 | 管理 Java 方法执行，每个方法调用创建一个栈帧 |
| 栈帧结构 | 局部变量表 → 操作数栈 → 动态链接 → 方法返回地址 |
| StackOverflow | 栈深度超限（如递归过深） |
| OOM | 动态扩展时内存不足 |

**深度解析：栈中存的是引用还是对象？**

> 面试高频考点，核心结论：**栈中存储的不是对象，而是对象的引用。**

```java
MyObject obj = new MyObject();
```

- `obj` 是一个**引用**，存储在**栈**中
- `new MyObject()` 创建的实际对象，存储在**堆**中
- `obj` 指向堆中该对象的内存地址

内存布局：

```
┌───────── 栈（Stack）──────────┐    ┌────────── 堆（Heap）───────────────┐
│                                │    │                                    │
│  int a = 10                    │    │                                    │
│  ┌──────────┐                  │    │                                    │
│  │ a │  10  │  ← 基本类型直接存值│    │                                    │
│  └──────────┘                  │    │                                    │
│                                │    │    ┌──────────────────────┐        │
│  MyObject obj                  │    │    │    MyObject 实例      │        │
│  ┌──────────┐                  │    │    │  ┌────────────────┐  │        │
│  │ obj│ 0x3F │ ─────────────────────→ │  │ name: "test"   │  │        │
│  └──────────┘   引用指向       │    │    │  │ age: 25        │  │        │
│                                │    │    │  └────────────────┘  │        │
│                                │    │    └──────────────────────┘        │
└────────────────────────────────┘    └────────────────────────────────────┘
```

引用的三种实现方式：

| 实现方式 | 说明 |
|----------|------|
| **句柄** | 引用存句柄表索引，句柄表再映射到实际地址（两级间接寻址） |
| **直接指针** | 引用直接存对象内存地址（HotSpot 默认，更快） |
| **压缩指针** | 64 位 JVM 用 4 字节表示引用（`-XX:+UseCompressedOops`，默认开启） |

> 面试回答建议：HotSpot 默认用**直接指针**，64 位 JVM 通过**压缩指针**把 8 字节引用压缩到 4 字节，节省内存。

**String 保存在哪里？**

字符串字面量（如 `"abc"`）存储在**字符串常量池**中。常量池中的字符串是不可变的，多个引用可以共享同一个字符串对象。

**字符串常量池的位置随 JDK 版本变化：**

| 版本 | 常量池位置 | 说明 |
|------|-----------|------|
| JDK 6 及更早 | **方法区（永久代）** | 与类元数据存放在一起 |
| **JDK 7+** | **堆（Heap）** | 移至堆中，便于 GC 回收不再使用的字符串 |

> JDK 7 将字符串常量池从永久代移到堆中，是因为永久代容量有限（默认约 82MB），大量字符串容易导致 `OutOfMemoryError: PermGen space`。移到堆中后，可以利用堆的 GC 机制自动回收不再引用的字符串。

**`String s = new String("abc")` 执行过程涉及哪些内存区域？**

这行代码的执行分两步：

1. **检查字符串常量池**：JVM 先检查常量池中是否已有 `"abc"`
   - **不存在** → 在常量池中创建 `"abc"` 的引用，再在堆中创建一个 `new String` 对象 → 共创建 **2 个对象**
   - **已存在** → 只在堆中创建一个 `new String` 对象 → 共创建 **1 个对象**
2. **变量 `s` 存储在栈中**，指向堆中新创建的 String 对象（不是指向常量池中的引用）

```
┌──── 栈 ────┐     ┌──────────── 堆 ─────────────┐
│             │     │                              │
│  s ──────────────→  new String("abc")  ← 堆对象  │
│             │     │                              │
│             │     │  字符串常量池（JDK7+ 在堆中）   │
│             │     │  ┌────────┐                  │
│             │     │  │ "abc"  │  ← 常量池引用     │
│             │     │  └────────┘                  │
└─────────────┘     └──────────────────────────────┘
```

**`"abc"` vs `new String("abc")` 核心区别：**

| 对比维度 | `String s = "abc"` | `String s = new String("abc")` |
|----------|--------------------|---------------------------------|
| 对象数量 | 常量池没有就创建 1 个，有就 0 个 | 一定在堆上新建 1 个（常量池可能 +1） |
| `s` 指向 | **常量池**中的 `"abc"` | **堆上**的新 String 实例 |
| 何时确定 | **编译期**确定，写入常量池 | **运行期**创建堆对象 |
| 编译器优化 | 相同字面量共享同一引用 | 每次 `new` 都是新对象，无法优化 |

```
String s1 = "abc":              String s2 = new String("abc"):

┌── 栈 ──┐  ┌──── 堆 ────┐     ┌── 栈 ──┐  ┌──── 堆 ────────┐
│         │  │             │     │         │  │                 │
│  s1 ───────→ "abc"（池） │     │  s2 ───────→ String 实例    │
│         │  │             │     │         │  │  常量池:"abc"   │
└─────────┘  └─────────────┘     └─────────┘  └─────────────────┘
```

```java
String a = "abc";
String b = "abc";
a == b;           // true  —— 编译期指向常量池同一个对象

String c = new String("abc");
String d = new String("abc");
c == d;           // false —— 两个不同的堆对象

a == c;           // false —— a 指向常量池，c 指向堆对象
a.equals(c);      // true  —— 内容相同
```

> **实践建议**：优先用字面量 `"abc"`，避免 `new String("abc")` 创建不必要的对象。

**对比三种创建方式：**

```java
String s1 = "abc";            // 1 个对象：常量池中的 "abc"，s1 指向它
String s2 = new String("abc"); // 1 或 2 个对象：堆中新对象，常量池可能新增
String s3 = s2.intern();      // 0 个新对象：返回常量池中 "abc" 的引用

s1 == s3;  // true  —— 都指向常量池同一个对象
s1 == s2;  // false —— s1 指向常量池，s2 指向堆中新对象
s2 == s3;  // false —— s2 指向堆对象，s3 指向常量池
```

### 3. 本地方法栈（Native Method Stack）

| 属性 | 说明 |
|------|------|
| 作用 | 为 Native 方法（C/C++ 实现）服务 |
| HotSpot | 与 Java 虚拟机栈合二为一 |
| OOM | 同虚拟机栈 |

---

## 二、线程共享区域（VM 启动时创建）

### 4. 堆（Heap）

| 属性 | 说明 |
|------|------|
| 作用 | GC 主战场，存放几乎所有对象实例和数组 |
| 分代 | 新生代（Eden + S0 + S1）+ 老年代 |
| 比例 | 新生代占堆 1/3，老年代占堆 2/3；Eden : S0 : S1 = 8 : 1 : 1 |
| OOM | `Java heap space` / `GC Overhead Limit Exceeded` |

**堆内存布局：**

```
┌────────────────────────────── Heap ──────────────────────────────────────┐
│                                                                          │
│  ┌───────── 新生代 Young（占堆 1/3）──────────┬─── 老年代 Old（占堆 2/3）───┐│
│  │                                             │                            ││
│  │  ┌─────────┐  ┌────┐  ┌────┐               │  ┌──────────────────────┐  ││
│  │  │  Eden   │  │ S0 │  │ S1 │               │  │                      │  ││
│  │  │  (80%)  │  │from│  │ to │               │  │    Old Generation    │  ││
│  │  │         │  │    │  │    │               │  │   （长存活对象）       │  ││
│  │  └─────────┘  └────┘  └────┘               │  │                      │  ││
│  │                                             │  └──────────────────────┘  ││
│  └─────────────────────────────────────────────┴────────────────────────────┘│
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

**新生代（Young Generation）**

新创建的对象优先分配在 Eden 区（Eden : S0 : S1 = 8 : 1 : 1，通过 `-XX:SurvivorRatio` 控制）。

**S0 / S1（Survivor 区）的作用：**

S0 和 S1 是两个大小相等的 Survivor 区（也叫 From / To），核心作用是**在 Minor GC 之间保留存活对象**，同时让复制算法能高效回收且**不产生内存碎片**。

Minor GC 发生时，JVM 把 Eden + From 中所有存活对象复制到 To 区，然后清空 Eden 和 From。复制完成后 From 和 To 角色互换——原来的 To 变成下次的 From，原来的 From 变成下次的 To。

```
第 1 次 Minor GC 前：Eden(新对象)  | S0(From, 有存活) | S1(To, 空)
第 1 次 Minor GC 后：Eden(空)      | S0(空→变 To)     | S1(From, 存活)

第 2 次 Minor GC 前：Eden(新对象)  | S0(To, 空)       | S1(From, 有存活)
第 2 次 Minor GC 后：Eden(空)      | S0(From, 存活)   | S1(空→变 To)
```

**为什么需要两个 Survivor 而不是一个？** 复制算法需要两块空间来回倒腾。如果只有一个 Survivor，Eden 复制过来的存活对象会和 Survivor 原有对象混在一起，产生碎片，失去复制算法"无碎片"的优势。

**Minor GC 扫描范围：Eden + From 一起扫**

Minor GC 同时扫描 Eden 和当前的 From 区（不是 S0 和 S1 一起扫，而是 Eden + From 一起扫）。具体流程：

1. 从 GC Roots（栈变量、静态字段、常量等）出发，沿引用链遍历
2. 标记 Eden + From 中所有可达的对象为"存活"
3. 把存活对象一起复制到 To 区
4. 整体清空 Eden 和 From
5. From / To 角色互换

```
GC Roots（栈、静态变量等）
    │
    ▼
┌──────────────┐  ┌──────┐       ┌──────┐
│    Eden      │  │ From │       │  To  │
│  扫描+复制 ─────────────────→   │ ←接收│
└──────────────┘  └──────┘       └──────┘
     清空             清空          变成下次的 From
```

From 必须一起扫，因为里面存的是上一轮 GC 幸存下来的对象，可能被 Eden 中的新对象引用。如果不扫 From，这些对象会被误判为不可达而丢失。Eden 和 From 被当作一个整体——"当前新生代中所有正在使用的区域"。

**对象何时离开 Survivor 区？**

- **年龄达标晋升**：对象每经历一次 GC 年龄 +1，达到 `-XX:MaxTenuringThreshold`（默认 15）时晋升老年代
- **Survivor 放不下**：To 区空间不足时，剩余对象直接进老年代
- **动态年龄**：Survivor 区同年龄对象总大小超过 Survivor 空间一半时，≥ 该年龄的对象直接晋升

**老年代（Old Generation / Tenured Generation）**

经历过多次 Minor GC 仍然存活的对象会晋升到老年代。老年代空间通常比新生代大（占堆的 2/3），存放长生命周期对象。老年代触发 **Major GC / Full GC**，频率比 Minor GC 低但停顿时间更长。

**元空间（Metaspace）**

Java 8 起，永久代（PermGen）被元空间替代。元空间存储类元数据（字段、方法信息等），**不属于 Java 堆**，而是使用本地内存（Native Memory），解决了永久代容易 OOM 的问题。通过 `-XX:MaxMetaspaceSize` 设置上限。

**大对象（Humongous Objects）**

在 G1 收集器中，大小超过 **Region 一半**的对象被归类为大对象。大对象直接分配到连续的 Humongous Region 中（逻辑上属于老年代），跳过 Eden 和 Survivor 区，避免 young GC 时昂贵的复制开销。在 Parallel / CMS 收集器中，超过 `-XX:PretenureSizeThreshold` 的对象也会直接分配到老年代。

### 5. 方法区（Method Area）

| 属性 | 说明 |
|------|------|
| 作用 | 存储类元数据、方法字节码、常量池 |
| OOM | `java.lang.OutOfMemoryError: Metaspace` |

**子区域：**

- **运行时常量池**：编译期生成的字面量和符号引用，受方法区容量限制
- **字符串常量池**：JDK 1.7 起从永久代移至 Java 堆中，便于 GC 回收

**JDK 版本演进：**

| 版本 | 实现方式 | 说明 |
|------|----------|------|
| JDK 1.7 及更早 | 永久代（PermGen） | `-XX:MaxPermSize` 限制，约 82MB |
| JDK 1.7（过渡） | 永久代仍在 | 字符串常量池和静态变量移至堆 |
| **JDK 1.8+** | **元空间（Metaspace）** | 使用本地内存，`-XX:MaxMetaspaceSize` 设置上限 |

> 变更原因：永久代容量受限、GC 效率低、HotSpot 与 JRockit 合并（JRockit 无 PermGen）

---

## 三、非 JVM 规范区域

### 直接内存（Direct Memory）

| 属性 | 说明 |
|------|------|
| 来源 | JDK 1.4 NIO 引入，通过 `DirectByteBuffer` 在堆外分配 |
| 作用 | 减少 I/O 场景下 JVM 堆与 Native 内存之间的数据拷贝 |
| 限制 | 物理内存 + `-XX:MaxDirectMemorySize` |
| 场景 | NIO 文件读写、Netty、高性能缓存 |
| OOM | `OutOfMemoryError: Direct buffer memory` |

---

## 四、OOM 速查表

| 内存区域 | 线程属性 | 可能 OOM | 常见错误信息 | 常见原因 |
|----------|----------|----------|-------------|----------|
| 程序计数器 | 私有 | **不会** | — | — |
| 虚拟机栈 | 私有 | **会** | `StackOverflowError` | 递归过深、栈帧过大 |
| 本地方法栈 | 私有 | **会** | `StackOverflowError` | 同虚拟机栈 |
| 堆 | 共享 | **会** | `Java heap space` | 对象过多、内存泄漏 |
| 方法区 | 共享 | **会** | `OutOfMemoryError: Metaspace` | 类加载过多 |
| 直接内存 | — | **会** | `Direct buffer memory` | NIO Buffer 分配过多 |

---

## 五、面试高频追问

**Q1：为什么 JDK 1.8 要把永久代替换为元空间？**
> ① 永久代容量受限（`-XX:MaxPermSize`），容易 OOM；② 永久代 GC 效率低；③ HotSpot 与 JRockit 合并，JRockit 没有 PermGen，统一为 Metaspace 更合理。

**Q2：字符串常量池为什么从方法区移到堆？**
> JDK 1.7 将字符串常量池移至 Java 堆，因为堆有 GC 机制，可以更高效地回收不再使用的字符串，避免永久代 OOM。

**Q3：堆和栈的区别？栈中存的是对象还是引用？**
> 堆存对象（GC 管理，共享）；栈存局部变量和方法调用（自动回收，私有）。**栈中存的是对象的引用（地址），不是对象本身。** 基本类型直接存值在栈中，对象通过引用指向堆中的实际数据。详见上方"深度解析"小节。

**Q4：什么情况下会触发 Full GC？**
> ① 老年代空间不足；② 方法区空间不足；③ 显式调用 `System.gc()`（建议而非强制）；④ CMS concurrent mode failure。

---

## 六、JVM 本地内存（Native Memory）

JVM 本地内存是指 JVM 进程在操作系统上申请的、**不属于 Java 堆**的那部分内存。它由 JVM 自身和操作系统共同管理，**不受 GC 直接回收**。

> 面试中"本地内存"和"堆外内存"经常混用，但严格来说：本地内存是操作系统视角的概念（JVM 进程占用的所有非堆内存），堆外内存是 Java 视角的概念（不在 Java 堆里的内存）。两者高度重叠。

**本地内存的组成：**

```
┌──────────────────── JVM 进程内存（RSS）────────────────────┐
│                                                            │
│  ┌──────────────── Java 堆 ────────────────┐               │
│  │  Eden + S0 + S1 + Old（GC 管理）         │               │
│  └──────────────────────────────────────────┘               │
│                                                            │
│  ┌──────────────── 本地内存 ────────────────┐               │
│  │                                          │               │
│  │  ① Metaspace（元空间）                    │               │
│  │     类元数据、常量池、方法信息              │               │
│  │     -XX:MaxMetaspaceSize（默认不限）       │               │
│  │                                          │               │
│  │  ② 线程栈（每个线程一份）                  │               │
│  │     默认 1MB/线程，-Xss 调整               │               │
│  │     1000 线程 ≈ 1GB                      │               │
│  │                                          │               │
│  │  ③ Code Cache（JIT 编译产物）              │               │
│  │     -XX:ReservedCodeCacheSize             │               │
│  │                                          │               │
│  │  ④ Direct Memory（直接内存）               │               │
│  │     NIO DirectByteBuffer                  │               │
│  │     -XX:MaxDirectMemorySize               │               │
│  │                                          │               │
│  │  ⑤ JVM 自身开销                           │               │
│  │     GC 数据结构、类加载器、符号表            │               │
│  │                                          │               │
│  └──────────────────────────────────────────┘               │
└────────────────────────────────────────────────────────────┘
```

**各部分详解：**

| 组成 | 说明 | 关键参数 |
|------|------|----------|
| **Metaspace（元空间）** | 存放类元数据、常量池、方法信息。JDK8 取代永久代，使用本地内存 | `-XX:MaxMetaspaceSize`（默认不限） |
| **线程栈** | 每个 Java 线程独立的 native 栈，存放栈帧、局部变量表。线程数多时占用很大 | `-Xss`（默认 1MB） |
| **Code Cache** | JIT 编译器将热点代码编译为机器码后存储在这里 | `-XX:ReservedCodeCacheSize` |
| **Direct Memory** | NIO 的 `DirectByteBuffer` 在堆外分配，读写时避免堆内外拷贝，适合 IO 密集场景 | `-XX:MaxDirectMemorySize` |
| **JVM 自身开销** | GC 数据结构（如 Card Table）、类加载器元数据、符号表、JIT 编译器内部结构 | — |

**为什么本地内存泄漏不容易发现？**

Java 堆有 GC 自动回收，而本地内存需要手动释放或依赖 JVM 内部机制（如 `Cleaner`）。本地内存泄漏不会触发常规 GC，只会在物理内存耗尽时被操作系统 OOM Killer 杀掉，表现为进程突然消失，没有 Java 异常栈。

**排查本地内存的常用工具：**

| 工具 | 用途 | 示例 |
|------|------|------|
| **NMT（Native Memory Tracking）** | JVM 内置，按类别统计本地内存 | `-XX:NativeMemoryTracking=detail` + `jcmd <pid> VM.native_memory summary` |
| **pmap** | 操作系统级，查看进程内存映射分布 | `pmap -x <pid>` |
| **jemalloc** | 第三方内存分配器，带 profiling 报告 | `MALLOC_CONF=prof:true` 启动后生成 `.heap` 文件 |
| **jcmd** | 查看 GC 堆外内存概览 | `jcmd <pid> GC.heap_info` |

**NMT 输出示例（关键行）：**

```
Total: reserved=2147483648, committed=524288000
-                 Java Heap: reserved=1073741824, committed=268435456
-                     Non-Class: reserved=268435456, committed=134217728
-                       Class: reserved=134217728, committed=67108864   ← Metaspace
-                    Thread: reserved=104857600, committed=104857600   ← 线程栈（100线程×1MB）
-                      Code: reserved=251658240, committed=8388608     ← Code Cache
-                        GC: reserved=16777216, committed=4194304
-                 Internal: reserved=4194304, committed=2097152
-                    Symbol: reserved=8388608, committed=4194304
```

> **面试回答建议**：JVM 本地内存主要包括 Metaspace（类元数据）、线程栈（每线程 1MB）、Code Cache（JIT 产物）、Direct Memory（NIO 堆外缓冲）和 JVM 自身开销。排查用 NMT（`jcmd VM.native_memory`）或操作系统工具 `pmap`。本地内存泄漏比堆内存更隐蔽，因为不受 GC 管理。
