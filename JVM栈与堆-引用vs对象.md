# JVM 栈与堆：引用 vs 对象

> 面试高频考点：栈中存的到底是指针还是对象？

---

## 核心结论

**栈中存储的不是对象，而是对象的引用。**

```java
MyObject obj = new MyObject();
```

- `obj` 是一个**引用**，存储在**栈**中
- `new MyObject()` 创建的实际对象，存储在**堆**中
- `obj` 指向堆中该对象的内存地址

---

## 栈（Stack）存什么

| 类型 | 示例 | 说明 |
|------|------|------|
| 基本类型值 | `int a = 10` | 直接存值（4 字节） |
| 对象引用 | `MyObject obj = new MyObject()` | 存引用地址（64 位系统约 8 字节） |

栈管理的是**局部变量**和**方法调用上下文**（栈帧）。每个方法调用创建一个栈帧，方法返回时栈帧自动弹出回收。

## 堆（Heap）存什么

| 类型 | 示例 | 说明 |
|------|------|------|
| 对象实例 | `new MyObject()` | 所有通过 `new` 创建的对象 |
| 数组 | `new int[10]` | 所有数组，无论基本类型还是引用类型 |

堆是 GC 管理的区域，对象的创建和销毁由垃圾回收器负责。

---

## 内存布局图

```
┌───────── 栈（Stack）──────────┐    ┌────────── 堆（Heap）───────────────┐
│                                │    │                                    │
│  int a = 10                    │    │                                    │
│  ┌──────────┐                  │    │                                    │
│  │ a │  10  │  ← 基本类型直接存值│    │                                    │
│  └──────────┘                  │    │                                    │
│                                │    │    ┌──────────────────────┐        │
│  MyObject obj                  │    │    │    MyObject 实例      │        │
│  ┌──────────┐                  │    │    │  ┌────────────────┐  │        │
│  │ obj│ 0x3F │ ─────────────────────→ │  │ name: "test"   │  │        │
│  └──────────┘   引用指向       │    │    │  │ age: 25        │  │        │
│                                │    │    │  └────────────────┘  │        │
│                                │    │    └──────────────────────┘        │
│                                │    │                                    │
└────────────────────────────────┘    └────────────────────────────────────┘
```

---

## 面试追问：引用到底是什么？

**引用本质上是一个值**，这个值代表了对象在堆中的内存地址（可以理解为"指针"）。

不同 JVM 实现中，引用的具体形式不同：

| 实现方式 | 说明 |
|----------|------|
| **句柄** | 引用存的是句柄表索引，句柄表再映射到实际地址（两级间接寻址） |
| **直接指针** | 引用直接存对象内存地址（HotSpot 默认，更快） |
| **压缩指针** | 64 位 JVM 用 4 字节表示引用（`-XX:+UseCompressedOops`，默认开启） |

> 面试回答建议：HotSpot 默认用**直接指针**，64 位 JVM 通过**压缩指针**把 8 字节引用压缩到 4 字节，节省内存。

---

## 面试追问：String 存在栈还是堆？

```java
String s1 = "hello";           // 字符串常量池（JDK7+ 在堆中）
String s2 = new String("hello"); // 堆中创建新对象
String s3 = s2.intern();       // 返回常量池中的引用
```

- `"hello"` 字面量 → **字符串常量池**（JDK 7+ 在堆中，JDK 6 在方法区永久代）
- `new String()` → **堆**中创建新对象
- 字符串常量池的目的是**复用相同字符串**，减少内存占用

---

## 一句话总结

> **栈存引用（地址），堆存对象（数据）。** 基本类型是例外——直接存在栈里。
